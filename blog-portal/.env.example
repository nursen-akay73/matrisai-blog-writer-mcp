# Google AI Studio → https://aistudio.google.com/apikey
GEMINI_API_KEY=buraya_yapistir
GEMINI_MODEL=gemini-2.5-flash

# Cron / kuyruk (Render Cron Job → POST /api/cron/run-next)
# CRON_SECRET=uzun-rastgele-string

# --- Veritabanı ---
# Yerel demo (varsayılan): SQLite → blog-portal/data/blogs.db
#
# Neon / Postgres (canlı):
# DATABASE_URL=postgresql://USER:PASS@HOST/DB?sslmode=require
#
# Yerel Docker Postgres:
# DATABASE_URL=postgresql://blog:blog@127.0.0.1:5432/blog_portal

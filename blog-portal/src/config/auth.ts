/** Local demo credentials — no backend auth. */
export const LOCAL_AUTH = {
  email: 'nursen.akay@matriksdata.com',
  password: 'admin1234',
} as const

export const AUTH_STORAGE_KEY = 'matriks-mcp-content-portal-auth'
